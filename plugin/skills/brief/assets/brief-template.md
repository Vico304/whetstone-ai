# 学习任务简报（提供给 guided-learning-tutor 的配套文档）

> 用途：每次调用 guided-learning-tutor / clarify 时，将本文档与学习材料一并提供（或让其读取本文件），
> 使插件无需重复询问即可获得学习者背景、材料性质说明和学习目标。
> 本文档是**任务背景说明**，不是待学习材料本身，请勿将其纳入课程内容或来源清单。
> 维护者：{{学习者姓名或标识}}。学习进展变化时更新第五节。
> 生成方式：由 brief 技能于 {{date}} 生成初稿，学习者可（且应）自行细化修改；标注 `（默认值，可修改）` 的字段尤其如此。

---

## 一、学习者画像

- **背景**：{{与材料直接相关的经验自述；零基础/有部分基础/横向迁移}}
- **学习情境**：{{为什么学：接手工作 / 兴趣 / 备考 / 技术选型评估……}}
- **已有基础**（供前置诊断参考，仍请实际验证而非默认成立）：
  - {{相关领域 1：自述水平}}
  - {{相关领域 2：自述水平，未接触的明确写"预期是主要缺口"}}
- **偏好**：
  - 前置检查：`prerequisite_check={{always|auto|skip}}`（默认值，可修改）；
  - {{教学节奏偏好，如：逐节教学，一次一节；不懂倾向 DEEPEN 而不是跳过}}；
  - {{概念处理约定，如：疑问用 [[wikilink]] 标记，定期调用 clarify}}；
  - {{反馈风格，如：直接指出误解，不需要客套}}。

## 二、材料清单与性质说明

材料位置：`{{材料根目录绝对路径}}`

| 文档/目录 | 性质 | 处理提示 |
|---|---|---|
| {{文件名}} | {{教程/工作日志/论文/代码库/未完成稿…（规模）}} | {{该材料的阅读注意事项}} |
| {{空或未完成文件}} | 空文件/未完成稿 | 不作为材料输入{{；如适用：将作为结课迁移任务}} |

**对这批材料的统一处理要求**：

1. {{组织方式说明：如"按时间书写的日志，请按因果链重组课程"}}
2. {{证据区分要求：如"区分探索过程 / 已确认结论 / 未决问题"}}
3. {{不可访问资源：如"内部链接不可访问，按范围限制如实报告"}}
4. {{隐私要求：如"主机名/IP/密钥记录不复制进教学产物"}}
5. 材料未覆盖的知识需外部补充时，按插件规则标记 `external` 并给出可审核来源。

## 三、总体学习目标

**终点能力**（约 {{预估周期}} 后）：
1. {{可验收的行为描述："能解释 X 为什么存在"，不写"了解 X"}}
2. {{…}}

**反目标**（不追求）：
- {{明确排除项，防止课程范围膨胀}}

## 四、分阶段调用方式

### 课程包 1：{{阶段名}}（{{预估时长}}）

```
使用 guided-learning-tutor 学习以下材料，配套背景见本简报（学习任务简报.md）。

材料：
- {{材料相对路径列表}}

模式：build + teach；prerequisite_check={{值}}。
学习目标：见简报第三节 {{条目编号}}。
课程目录：{{工作区}}/01-{{阶段名}}/
```

验收：{{该阶段的可验收标准}}

### 课程包 2：{{阶段名}}（{{预估时长}}，{{启用条件}}）

```
{{同上结构；如依赖前一课程包，写明"前置：已完成课程包 01，进度见其 learning-progress.json"}}
```

### 恢复学习（每次新会话）

```
resume {{工作区}}/<当前课程包>/
```

### 概念补漏（每完成 2~3 节）

```
调用 clarify，处理 {{工作区}}/<当前课程包>/ 中的未解决 [[wikilink]] 和 concepts/_inbox.md
```

## 五、学习状态（随进展手动更新）

- 当前阶段：{{初始为课程包 1 未开始}}
- 已完成课程包：无
- 已确认掌握的概念：无（以各课程包 learning-progress.json 为准）
- 待办确认事项：{{影响计划的开放问题，如硬件/权限/联系人/期限}}
