# 学习计划（learning-plan.md）

> 扫描目录：`{{root}}`　日期：{{date}}　目的：{{①学上游 / ②巩固自己做过的 / ③接手别人的}}
> 由 guide 生成、逐门确认后写入；学习者可随时修改。它不是课程包；每门课开始时由 outline 阶段生成该课的大纲。清单原始数据见 `survey/materials-survey.json`。

## 1. 结论

先学 **{{第一门课名}}**（原因一句话）。共 {{N}} 门课，顺序见 §3；每门标注确认状态。排除了 {{M}} 个条目，理由见 §2。

## 2. 材料定级

| 条目 | 级 | 理由 | 用法 |
|---|---|---|---|
| `{{path}}` | A | 上游源码，README + docs 齐全 | 课程 1 来源（限 `src/xxx` 子系统） |
| `{{path}}` | B（待你确认） | 你 09-04 写的实施方案，标题编号、无会话信号 | 课程 2 来源 |
| `{{path}}` | C | 会话交接文档 | 只作导航；其中"待做事项"可做迁移题素材 |
| `{{path}}` | D | 2 GB 日志与数据 | 排除 |

## 3. 建议的课程序列

### 课程 1：{{名称}}　状态：{{已确认 / 待确认 / 暂不学}}

- 材料（具体路径）：`{{…}}`、`{{…}}`
- 目标：能解释 {{…}}，并能 {{…}}
- 规模估计：{{≈ 20 KB / ≈ 1800 行、6 个文件}} → 预计 {{5}} 个 unit
- 模式建议：{{完整 / 快速}}（理由）
- 前置依赖：{{无 / 课程 0}}
- 调用：

  ```text
  /whetstone:learn 学习 {{路径列表}}，知识库目录 {{store}}，课程目录 {{whetstone/courses/xxx}}/。
  我希望学完后能 {{目标}}。
  ```

### 课程 2：…

### 取向为 domain 时的形状（替代上面的课程列表）

**骨架课 {{skeleton-1}}**　状态：{{待确认}}　`shape: skeleton`
- 证据池（`pool`）：`{{occlum/docs}}`、`{{occlum/src/libos}}`；留给分支（`reserve`）：`{{teeapp/src/ra}}`；不用：`{{文档/…}}`（C 级）
- 目标：不看具体实现，能解释 {{领域}} 的基本原理链；学完后选分支
- 规模：由原理依赖图决定（不设上限）；模式建议：完整

**分支候选**（骨架课结束后再选，现在不确认）

| 候选 | 体现的原理 | 材料 | 与实际工作 |
|---|---|---|---|
| {{项目里的远程证明链路}} | {{远程证明、度量}} | `{{teeapp/src/ra}}` | {{你负责的 RA 校验逻辑}} |

### 3a. 前置栈（学习中自动维护；没有前置课时删掉本节）

```text
{{llm-inference-basics-1}}（depth 0，卡在 {{u01}}）
  ← {{transformer-forward-1}}（depth 1，教学中）
      ← {{linear-algebra-min-2}}（depth 2，已完成 {{2026-09-12}}）
```

每层：`lesson_id`、depth、卡在父课哪一节、状态（待确认大纲 / 教学中 / 已完成 / 未完成）。前置课的课程目录与父课同级。

## 4. 风险提示

- {{C 级文档里"结论摘要"的说法未经一手来源核实，课程不会引用}}
- {{`teeapp-sgx-rwa-release` 与 `trustflow-teeapps-rwa` 内容重叠，只取源码树}}

## 5. 进度

| 课程 | 状态 | 课程目录 |
|---|---|---|
| 1 | 未开始 | `whetstone/courses/{{id}}/` |

## 6. 下一步

复制课程 1 的调用语句开课；想先看或改大纲就 `/whetstone:outline whetstone/courses/{{id}}/`。
